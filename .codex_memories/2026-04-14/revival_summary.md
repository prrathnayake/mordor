# Revival Summary

## Date
2026-04-14

## Restart Context
- Chrona Twin is a TypeScript monorepo for a browser-based geospatial digital twin platform with live monitoring, replay, alerting, evidence capture, and SWAN-oriented intelligence flows.
- The canonical full verification command remains `npm run validate`.
- The intended primary branch is `main`, but the active local checkout is still `master`; verify branch intent before any branch-sensitive work.
- Durable task memory must stay in `.codex_memories/`, and manual/debug artifacts belong under `tests/manual/outputs/`.
- No external docs hub is configured; local `docs/` remains the authoritative documentation surface.

## First-Task Review
- Reviewed the previous day memory folder at `.codex_memories/2026-04-13/`.
- Carry-forward risks remain the branch-name mismatch and the need to preserve unrelated user changes already present in the worktree when future branch or commit work happens.

## Watch Items
- The top-level structure is cleaner now: Docker and Compose assets live under `infra/`, and current-state docs are grouped under `docs/architecture`, `docs/runbooks`, and `docs/tests`.
- Repo gates still reference a subset of legacy numbered docs, so those root-level docs remain in place even though topical docs are now the preferred navigation path.
- `npm run lint` currently reports pre-existing formatting issues in existing source files outside this structural cleanup; treat that as baseline debt unless a later task intentionally runs a broader formatting sweep.

## Latest Completed Work
- 2026-04-13 09:56:17 +10:00: Refreshed the current-state project and architecture docs, aligned runtime port/default API-base behavior with the config contract, corrected nearest-source distance calculations to meters in persistence and SWAN repository queries, and hardened malformed JSON handling in the API to return a client error instead of a server error.
- 2026-04-13 11:46:05 +10:00: Completed a deeper review/fix cycle across API shutdown, SWAN/UI flows, external-data adapters, and automated tests; stabilized shared Postgres testcontainers usage, fixed external-data retry timing and stale unit expectations, updated multiple Playwright specs to match current UI behavior, and finished with a green full `npm run validate`.
- 2026-04-14 12:12:55 +10:00: Reorganized the repo structure by moving Docker/Compose support out of the root and into `infra/compose` and `infra/docker`, relocated current-state docs into topical `docs/architecture`, `docs/runbooks`, and `docs/tests` folders, refreshed the root and docs indexes to match the new navigation model, and confirmed the required docs gate still passes.
- 2026-04-14 12:19:23 +10:00: Confirmed the repo already contains real live-data plumbing for flights and external layers, including the API live-world service, SSE stream, OpenSky polling, CelesTrak/USGS/NOAA/traffic adapters, and Cesium rendering hooks in the web client; the next likely implementation step is to push external-layer refreshes into the live event stream so the UI updates incrementally instead of mostly minute-by-minute polling.
- 2026-04-14 12:25:15 +10:00: Added `external_layer_update` to the shared `/live/events` SSE contract and wired external layer refreshes to publish it, so enabled browser layers now refresh in response to live notifications instead of waiting for the periodic metadata poll; focused verification passed with `npm run typecheck` and the `live-resilience` integration spec.
- 2026-04-14 12:35:23 +10:00: Tightened the front-end live experience by reconciling live flight and satellite entities in place and showing per-layer relative freshness timestamps in the left rail; the remaining high-value next step would be viewport-aware subscriptions or bounded API queries so the browser stops loading world-scale overlays when the operator is focused on a region.
- 2026-04-14 12:40:33 +10:00: Implemented the bounded-query step too: `/layers/:layerId/data` now accepts `west/south/east/north`, persistence filters `external_data_events` to the viewport, and the Cesium client re-requests enabled layers after camera movement using the current view rectangle, reducing over-fetch/render for world-scale overlays.
- 2026-04-14 12:46:04 +10:00: Completed the next live-scaling step by making `/live/events` materialize viewport-scoped external-layer snapshots per subscriber and reconnecting the browser SSE subscription when viewport bounds change, so future overlay pushes are region-aware without an extra fetch roundtrip after every refresh.
- 2026-04-14 12:50:28 +10:00: Completed the next optimization too: once a subscriber has its initial scoped overlay snapshot, later live refreshes can arrive as `external_layer_delta_update` messages with only upserts and removals, and the browser now maintains a per-layer event cache so it can apply those differential updates safely.
- 2026-04-14 12:24:28 +10:00: Completed a source-landscape review and external research pass: the repo currently ships OpenSky live flights plus USGS/CelesTrak/NOAA/CityBikes/conditional traffic adapters and an explicitly unavailable military layer, while the most valuable next additions for global analysis are maritime vessel intelligence, wildfire hotspots, global disaster feeds, space weather, air quality, and carefully bounded global event/news signals.
- 2026-04-14 12:28:40 +10:00: Improved the tactical globe's zoom capability by replacing the fixed `NaturalEarthII` demo texture with a configurable basemap provider path delivered through the web server, using sharper ArcGIS World Imagery by default and allowing OSM/custom XYZ tiles when needed; updated the local runbook/config docs and verified with `npm run typecheck` plus `npx vitest run tests/integration/tactical-shell.spec.ts`.
- 2026-04-14 12:40:40 +10:00: Added a right-rail `MAP SURFACE` control to switch the globe between satellite and street basemaps at runtime; the implementation swaps only the base imagery layer so tactical entities and overlays stay rendered, and focused web verification remained green with `npm run typecheck` plus `npx vitest run tests/integration/tactical-shell.spec.ts`.
- 2026-04-14 12:44:40 +10:00: Extended the UI with a safe, optional ground-view path: the web app now accepts `STREET_SCENE_PROVIDER`, `CESIUM_ION_TOKEN`, and `GOOGLE_MAPS_API_KEY`, configures Cesium 3D tiles support when present, and exposes an inspector-level `ENTER GROUND VIEW` action for positioned objects while keeping the button disabled by default so existing deployments and tests are not destabilized.
- 2026-04-14 12:40:05 +10:00: Traced the current extension seams for adding new always-on live sources, ingestion workers, persistence, SSE/live event publishing, capture/evidence storage, and the present UI source/layer rendering path; the repo is still largely hardcoded by source/layer switch statements and specific route handlers, so new source families would currently enter through those contracts and service entry points rather than a plugin registry.
- 2026-04-14 12:39:54 +10:00: Reviewed the web client's architecture for safe autonomous UI expansion. The app is a single Cesium-based imperative shell in `apps/web/public/app.js` with static HTML panels in `apps/web/public/index.html`, state buckets for layers/incidents/inferences/SWAN, hardcoded layer and panel renderers, and a narrow data-driven seam through SWAN projections plus existing `/layers`, `/incidents`, `/alerts`, `/events`, and `/live/events` APIs.
- 2026-04-14 12:41:32 +10:00: Combined those repo constraints with fresh source research into a next-stage operating model: add always-on worker agents for authoritative events, conflict/news context, and media discovery; normalize everything into incidents plus linked evidence/media artifacts; use the live event bus and SWAN projections to update the dashboard; and keep autonomous UI growth schema-driven instead of allowing arbitrary generated markup or direct self-modifying frontend code.

- 2026-04-14 12:58:59 +10:00: Strengthened the live overlay verification story with more complex `live-resilience` scenarios covering reconnect backfill semantics against current persisted state, per-layer snapshot isolation on a shared SSE stream, and viewport-bounded removals versus out-of-bounds changes; focused verification passed with `npm run typecheck` and `npx vitest run tests/integration/live-resilience.spec.ts tests/integration/external-data-api.spec.ts`.
